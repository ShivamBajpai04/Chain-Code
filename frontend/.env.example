# local development
VITE_DOMAIN=http://localhost:5000/api

# production (set in Vercel dashboard, frontend service):
# VITE_DOMAIN=/api

# code execution runs server-side now (backend/.env: EXECUTION_PROVIDER,
# RAPIDAPI_KEY or SELFHOSTED_JUDGE0_URL). The browser never touches Judge0.
